{
  "name": "msw-grpc-gen",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "rome check . && rome format . --write",
    "lint:ci": "rome ci .",
    "test": "npm t --workspaces --if-present"
  },
  "devDependencies": {
    "rome": "12.1.3"
  }
}
